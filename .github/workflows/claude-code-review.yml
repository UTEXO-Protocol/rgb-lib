name: AI Review - Claude

on:
  pull_request:
    types: [review_requested, synchronize]
  workflow_dispatch:
    inputs:
      pr_number:
        description: "Pull request number to review (for manual reruns)"
        required: true
        type: string

jobs:
  claude-review:
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event.action == 'review_requested' &&
       github.event.requested_team.slug == 'ai-review-claude') ||
      (github.event.action == 'synchronize' &&
       contains(github.event.pull_request.requested_teams.*.slug, 'ai-review-claude'))
    runs-on: ubuntu-latest
    concurrency:
      group: ai-review-claude-pr-${{ github.event_name == 'workflow_dispatch' && inputs.pr_number || github.event.pull_request.number }}
      cancel-in-progress: true
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        uses: anthropics/claude-code-action@v1
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_API_KEY }}
          show_full_output: false
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event_name == 'workflow_dispatch' && inputs.pr_number || github.event.pull_request.number }}

            Please review this pull request and provide feedback on:
            - Code quality and best practices
            - Potential bugs or issues
            - Performance considerations
            - Security concerns
            - Test coverage

            Use the repository's CLAUDE.md for guidance on style and conventions.
            Be constructive and helpful in your feedback.

            Use `gh pr comment` to publish your final review on the PR.
          claude_args: '--allowed-tools "Bash(gh issue view:*),Bash(gh search:*),Bash(gh issue list:*),Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*),Bash(gh pr list:*)"'
